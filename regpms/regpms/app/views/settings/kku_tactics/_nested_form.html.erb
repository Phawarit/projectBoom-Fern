<table class="table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th nowrap><%= KkuTactic.human_attribute_name :kku_strategic %></th>
            <th nowrap><%= KkuTactic.human_attribute_name :no %></th>
            <th nowrap><%= KkuTactic.human_attribute_name :name %></th>
            <% unless action_name == "show" %>
                <th style="width:30px;">
                    <%= link_to_add_association f, :kku_tactics, partial: "/settings/kku_stratkku_tacticsegics/nested_fields", class: "btn btn-sm btn-success",
                    "data-association-insertion-node" => "#kku_tactic_fields",
                    "data-association-insertion-method" => :append do %>
                    <i class='fa fa-plus'></i>
                    <% end %>
                </th>
            <% end %>
        </tr>
    </thead>
    <tbody id="kku_tactic_fields">
        <%= f.simple_fields_for :kku_tactics do |kku_tactic| %>
            <%= render '/settings/kku_tactics/nested_fields', :f => kku_tactic %>
        <% end %>
    </tbody>
</table>
<script type="text/javascript">
    jQuery(function($) {
        $('#kku_tactic_fields').on('cocoon:after-insert', function(e, insertedItem) {
            init_input_date_picker();
            init_input_time_picker();
            init_input_datetime_picker();
        });
    });
</script>